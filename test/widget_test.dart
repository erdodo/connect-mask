// Basic widget test for Connect & Mask app

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App initialization test', (WidgetTester tester) async {
    // Basic test to ensure app compiles
    expect(true, isTrue);
  });
}
